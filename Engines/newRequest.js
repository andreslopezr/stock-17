function ConfirmDiscard(id)
{
    console.log("frgser0");
    var del = window.confirm("¿Realmente desea resolver esta solicitud?");
    if (del == null || del == "")
    {
        //cambiar atributo a la solicitud nosolved a solved 
    }
}